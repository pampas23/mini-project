
<%= form_for @player, :url => { :controller => 'players', :action => 'update', :id => @player } do |f| %>
	<%= render :partial => 'form', :locals => { :f => f } %>

    <%= f.submit "Update" %>
<% end %>